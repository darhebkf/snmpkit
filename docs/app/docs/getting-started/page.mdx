import { Tabs, Callout } from 'nextra/components'

# Getting Started

Install snmpkit and set up your environment.

## Installation

<Tabs items={['pip', 'uv', 'pdm']}>
  <Tabs.Tab>
```bash
pip install snmpkit
```
  </Tabs.Tab>
  <Tabs.Tab>
```bash
uv add snmpkit
```
  </Tabs.Tab>
  <Tabs.Tab>
```bash
pdm add snmpkit
```
  </Tabs.Tab>
</Tabs>

## Requirements

- **Python 3.10+** (3.14+ recommended for free-threading)
- **Net-SNMP** (for AgentX support)

### Installing Net-SNMP

<Tabs items={['Ubuntu/Debian', 'RHEL/CentOS', 'macOS']}>
  <Tabs.Tab>
```bash
sudo apt install snmpd snmp libsnmp-dev
```
  </Tabs.Tab>
  <Tabs.Tab>
```bash
sudo dnf install net-snmp net-snmp-utils net-snmp-devel
```
  </Tabs.Tab>
  <Tabs.Tab>
```bash
brew install net-snmp
```
  </Tabs.Tab>
</Tabs>

## Verify Installation

```python
import snmpkit
print(snmpkit.__version__)
```

## Configure snmpd for AgentX

To use snmpkit agents, enable AgentX in snmpd:

```conf filename="/etc/snmp/snmpd.conf"
# Enable AgentX master agent
master agentx
agentXSocket /var/agentx/master

# Allow read access
rocommunity public

# Allow write access (for SET operations)
rwcommunity private
```

Restart snmpd:

```bash
sudo systemctl restart snmpd
```

<Callout type="info">
The default AgentX socket is `/var/agentx/master`. Make sure your user has permission to connect to it.
</Callout>

## Next Steps

Choose your path:

- **[Agent Guide](/docs/agent)** — Build SNMP agents that expose data
- **[Agent Quickstart](/docs/agent/quickstart)** — Jump straight to code
